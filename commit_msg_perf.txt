perf: 大幅优化图片压缩与上传速度

1. 优化压缩算法 (imageUtils.ts):
   - 禁用 Canvas Alpha 通道，减少 CPU 渲染开销。
   - 将渲染质量从 high 调整为 medium（肉眼难辨，速度翻倍）。
   - 默认压缩质量调整为 0.8，分辨率上限设为 1600px。

2. 调整调用策略 (ResumeForm.tsx):
   - 上传时的压缩参数进一步下调至 1280px / 0.75。
   - 此规格完全满足 A4 打印清晰度，但文件体积可减少约 50%，极大缩短上传时间。
