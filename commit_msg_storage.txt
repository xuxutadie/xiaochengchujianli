feat: 实现后端图片存储功能并优化 LocalStorage 空间占用

1. 后端 (backend/index.js): 
   - 引入 multer 和 fs-extra 处理文件上传。
   - 新增 /api/upload 接口，将图片持久化存储至服务器 uploads/ 目录。
   - 开启静态资源服务，允许通过 URL 访问上传的图片。
   - 安装新依赖: multer, path, fs-extra。

2. 前端 (components/ResumeForm.tsx & App.tsx):
   - 升级图片上传逻辑：先本地压缩，再异步上传至后端。
   - 简历数据中优先存储图片的 URL 链接而非 Base64，大幅降低 LocalStorage 占用（从数 MB 降至数 KB）。
   - 保留 Base64 作为上传失败时的兜底方案。
   - 优化压缩参数 (1600px, 0.85 质量) 以平衡清晰度与体积。
   - 在预览区和表单中增加了存储空间已满的视觉警告和操作建议。
